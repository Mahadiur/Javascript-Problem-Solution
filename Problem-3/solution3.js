let firstAge=Number(prompt('Enter First Age:'));
let secondAge=Number(prompt('Enter Second Age:'));

if (firstAge > secondAge){
    console.log(`Elder Brother age is ${firstAge}`);
}else{
    console.log(`Elder Brother age is ${secondAge}`);
}